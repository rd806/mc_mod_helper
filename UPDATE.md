# 更新记录

## latest

添加：
* 多类型浏览：模组 / 整合包 / 材质包 / 光影 / 插件（可选类型随数据来源变化）。

修复：
* 打开Agent时默认返回当前已存在的空对话，同时优化了界面。

更新：
* 首页与探索页合并为「浏览页」：顶部一个可展开的筛选栏，按分类 / 游戏版本 / 排序方式组合筛选。
* 底栏页签调整为：首页 / AI / 我的 / 设置（设置页从首页右上角按钮改为页签）。
* 启动时的请求数从 5 个降到 3 个（筛选选项 ×2 + 列表第 1 页）。 
* 主题色种子的选择改为手动输入16进制RGB值。
* 输入弹窗固定宽度为300

## v1.1.0-release

> [!Warning]
> Windows端的默认文件存储位置已更改：`com.example/MCModHelper` -> `com.github.rd806/MCModHelper`
> 
> Android 安装包体积精简，按 CPU 架构分发安装包。

添加：
* 模组详情页增加“开发团队”卡片和“返回顶部”按钮。
* 设置页可选择字体。
* 浏览历史会被记录。
* 自动翻译：设置页打开开关后，模组详情页的正文会自动翻译成目标语言。
* 添加了一个简易的Agent（极不完善！！！），可在设置界面设置相关参数。

修复：
* 单个含图注图片可以正常渲染灯箱，所有图注均居中展示。
* 嵌套列表可以正常渲染。
* 搜索时点击来源按钮会正常高亮。

更新：
* 首页显示数据来源并可选择展示“默认排序”、“最新收录”、“最新编辑”。
* 详情页布局更新，“支持版本”卡片并入“加载环境”。
* 增加了版本号的显示大小。
* 设置界面移动到首页上方。

## v1.0.1-beta

添加：
* 模组收藏功能（使用 sqlite 本地数据库）。

修复：
* 模组详情页的描述现在可以正确回退。

更新：
* MC百科的模组检索均使用 `https://www.mcmod.cn/modlist.html` 接口。
* 模组卡片统一使用 `ModCard`。
* 宽屏详情页的右栏使用固定宽度。
* 含图片表格允许横向滚动。


## v1.0.0-release

项目首个发行版。

以下功能已经实现：
* 搜索、浏览模组 
* 切换数据来源