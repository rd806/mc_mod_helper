# 更新记录

## v1.0.1

添加
* 模组收藏功能（使用 sqlite 本地数据库）。

修复：
* 模组详情页的描述现在可以正确回退。

更新：
* MC百科的模组检索均使用 `https://www.mcmod.cn/modlist.html` 接口。
* 模组卡片统一使用 `ModCard`。
* 宽屏详情页的右栏使用固定宽度。
* 含图片表格允许横向滚动。


## v1.0.0

项目首个发行版。

以下功能已经实现：
* 搜索、浏览模组 
* 切换数据来源