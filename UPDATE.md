# 更新记录

## Lastest

> [!Warning]
> Windows端的默认文件存储位置已更改：`com.example/MCModHelper` -> `com.github.rd806/MCModHelper`

添加：
* 模组详情页增加“开发团队”卡片和“返回顶部”按钮。
* 设置页可选择字体。

修复：
* 单个含图注图片可以正常渲染灯箱，所有图注均居中展示。
* 嵌套列表可以正常渲染。
* 搜索时点击来源按钮会正常高亮。

更新：
* 详情页布局更新，窄屏界面可切换展示内容（模组介绍与其他信息），“支持版本”卡片并入“加载环境”。
* 增加了版本号的显示大小。
* 搜索界面的入口移至首页右下角。

## v1.0.1

添加：
* 模组收藏功能（使用 sqlite 本地数据库）。

修复：
* 模组详情页的描述现在可以正确回退。

更新：
* MC百科的模组检索均使用 `https://www.mcmod.cn/modlist.html` 接口。
* 模组卡片统一使用 `ModCard`。
* 宽屏详情页的右栏使用固定宽度。
* 含图片表格允许横向滚动。


## v1.0.0

项目首个发行版。

以下功能已经实现：
* 搜索、浏览模组 
* 切换数据来源